import { Injectable } from '@angular/core';

@Injectable({
    providedIn: 'root'
})

export class Absent {
    id: string;
    name: string;
    division: string;
    stage: string;
    date: string;
    tag: string;
}